@model IEnumerable<RockstarsManagementSquad.Models.SurveyViewModel>
@{
    ViewData["Title"] = "Surveys";
}

<div class="text-start">
    <form asp-controller="Surveys" method="post" asp-action="">
        <input type="text" name="name" placeholder="Enquête Name" />
        <br />
        <textarea name="discription" rows="4" cols="50" placeholder="Enquête discription."></textarea>
        <br />
        <input type="submit" name="submit" value="Submit" />
    </form>
</div>

<div>
    <br />
    <br />
    <table class="table">
        <tdead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Address</th>
            </tr>
        </tdead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.id</td>
                    <td>@item.name</td>
                    <td>@item.description</td>
                </tr>
            }
        </tbody>
    </table>
</div>
